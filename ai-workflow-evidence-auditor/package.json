{
  "name": "ai-workflow-evidence-auditor",
  "version": "0.1.0",
  "private": true,
  "description": "Private tool-only prototype for evidence-bound AI workflow review.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "audit": "tsx src/index.ts",
    "eval:live": "RUN_LIVE_EVAL=true vitest run tests/live-eval.test.ts"
  },
  "dependencies": {
    "openai": "^6.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  },
  "engines": { "node": ">=20.12" }
}
